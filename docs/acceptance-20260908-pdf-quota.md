# 2026-09-08 网站验收与发布记录

## 范围与授权

- 用户已明确授权本轮已验收网站改动推送 GitHub main、部署阿里云，以及启用每分钟额度维护服务。
- PDF 必须先通过真实转换验收才启用；不把单元测试或构建成功视为 PDF 视觉验收。
- 本轮工作在独立 worktree `.qa/website-release-20260908`。根工作区的网申插件改动未合并、未覆盖。
- 未调用 AI、未发送邮件、未删除或修改用户账号，也未使用真实用户简历作转换样本。

## 已上线且现场验证：额度维护

- 正式仓库和服务器源码版本：`b931c251478a656dbc481274f77fd29ce2a13df8`。
- 网站应用构建保持原版本；此次提交仅增加维护工具与文档，没有更换网页构建。
- 已安装独立无登录系统用户 `zhitu-quota`；每分钟 systemd timer 已启用。
- 首次服务结果：`Result=success`、`ExecMainStatus=0`。
- 服务内存上限 192 MiB、CPU 上限 25%、TasksMax 32；维护脚本仅调用既有额度 reconciliation RPC。
- 服务使用服务器现有 Supabase 服务端密钥；该密钥仍是高权限密钥，不应描述为数据库单 RPC 专用凭证。
- 独立凭证目录为 root:root 0700，凭证文件为 root:root 0600；原 `.env.local` 从 0644 收紧为 0600。未输出密钥内容。

验收窗口从 `2026-09-08T13:01:33.000Z` 开始。现场只读检查返回：

```json
{
  "event": "ai_quota_timer_verification",
  "successfulRuns": 40,
  "failedRuns": 0,
  "unexpectedApplicationLogs": 0,
  "spacedRuns": true,
  "hostBootRecoveryObserved": false,
  "firstCompletedAt": "2026-09-08T13:01:34.804Z",
  "lastCompletedAt": "2026-09-08T13:43:45.182Z",
  "lastCounts": {"completed": 0, "released": 0, "examined": 0, "skippedLocked": 0},
  "passed": true
}
```

- 同时 HTTP 健康检查返回 `{"ok":true,"service":"zhitutracker","mode":"production"}`。
- 没有重启服务器验证开机恢复；本窗口没有待处理额度记录，不能声称观察到了真实补扣/退回。
- 发现仅验收脚本存在 systemd 239 时间参数兼容问题：ISO `T/Z` 参数退出码 1，等价 `@epoch` 参数退出码 0。计时服务本身不受此问题影响。
- 正式目录未为此临时改代码；在服务器私有 QA 目录创建仅替换时间格式的验收副本，完成上述全套权限、运行间隔、日志核验。
- 永久兼容修复已提交为 PDF 分支 `697f83e`；另有独立维护发布分支 `codex/quota-systemd-239` 的 `6bb375676f913d5967a6be985362eb55c4577f1e`，其 main 推送尚未成功。

## 已实现、未上线：PDF

- 原位置应用用户已确认的文字，复用 DOCX 导出路径；不重新请求 AI，不额外扣次数。
- 转换前取得跨进程独占锁；下载、解压、模板替换纳入限时和并发保护。
- DOCX 解压大小、条目、路径、CRC 和伪造尺寸检查；不会在完整解压后才判断超限。
- 非 root、无网络、只读根目录 Docker 转换，固定镜像 ID，限制内存、CPU、进程数及时间；文字提取也在容器内。
- 一页 A4、文字层与确认文字一致性门槛；不满足就明确失败，不删改确认文字、不强行缩字号。
- PDF 失败保留 DOCX 与版本，支持重试；修复切换简历、离开工作区、响应乱序引起的旧内容下载/按钮锁死。
- PDF 字符检查不是全部 ATS 兼容性或视觉验收；双栏阅读顺序、页眉页脚和排版仍须实际查看 PDF。

## 本地验证证据

- 隔离网站基线 `703253c`：72 个测试文件、532 项测试通过（`src scripts integrations worker`，maxWorkers 2）。
- 独立 TypeScript 检查通过；`next build --webpack` 成功，36/36 静态页面生成，包含 PDF 接口。
- 后续离线官方镜像材料校验工具单独 8/8 测试通过，lint/diff check 通过。
- 不包括另一任务的 `browser-extension`；不能据此声称插件兼容性通过。
- 五份纯合成 DOCX 样本、真实转换驱动和溢出/非 A4 负例已准备；尚无成功的服务器实际转换或视觉证据。

## 发布状态与外部阻塞

- GitHub 正式 main 当前保持 `b931c25`，服务器额度服务已安装并验收通过。
- 远端 `codex/pdf-quota-production` 最后确认到 `8b1076cbf50a7225269971f6e2a95f25f2842369`。
- 本地后续 `703253c`（交付竞态修复）、`3b43026`（官方材料离线校验）已提交，推送遇连接重置/超时，不能描述为已推送。
- GitHub 连接器能只读，但创建树返回 403；没有绕过其权限或修改授权。
- 阿里云 Docker Hub 下载超时；官方 `raw.githubusercontent.com` 文件请求 15 秒无数据超时；同一 Debian 官方仓库固定 commit 的 Git 下载返回 Empty reply。未改用第三方镜像、代理或关闭 TLS。
- 未加载半成品镜像；未启用 PDF 环境变量或更换正式网页构建。

## 下一步

1. 恢复可用的官方文件/Git 传输，或另行确认可用的可信离线构建与传输环境。
2. 推送永久 systemd 验收修复；确认远端实际 SHA 后才更新服务器。若分支分叉，正常合并，不强推。
3. 根据 `docs/official-debian-archive.md` 获取固定官方材料，验证全部 SHA256/diffID，再加载基础镜像。
4. 用受限资源构建转换镜像，记录实际不可变镜像 ID。
5. 运行 `scripts/qa-resume-pdf-real.mjs`；检查一页/双栏/页眉页脚和负例，再逐页渲染查看。失败时不降低校验标准。
6. 通过后才推送并部署 PDF 网站版本；保留上一 `.next` 和回退路径，最后检查健康、权限、下载回退及额度不变。

所有临时验收目录只存代码或合成样本；本轮没有进行清理删除。不要把临时目录当作正式发布目录。

## 2026-09-09 续验收：生成、预览和下载补齐（未上线）

- 在隔离工作树 `.qa/website-release-20260908` 补充生成后的 PDF 预览、PDF/DOCX 下载和原文件下载；生成成功先预览，不自动触发下载。
- 同一生成版本的预览和下载复用本地 Blob，不重复调用转换或 AI；切换简历/离开页面取消请求、撤销 Blob URL、终止预览 worker 并清除画布。
- 历史版本即使对应分析不可读，仍保留下载入口。原文件接口按登录用户和所属简历双重筛选，私有下载、不缓存，不生成公开存储链接。
- 实测发现 `iframe sandbox=""` 的原生 PDF 预览为空白，已替换为按需加载的 PDF.js 画布预览（同站 worker，不加载 PDF 脚本、表单或链接交互）。真实合成 PDF 在内嵌浏览器显示成功；这是预览器验收，不是服务器 DOCX 转换验收。
- 最终画布预览修改后的全量回归：75 个测试文件、547 项通过；TypeScript、相关 lint 和 `next build --webpack` 通过，36/36 静态页面生成。
- 本地生产构建预览已恢复在 `http://127.0.0.1:3000/app`，未配置真实 Supabase 身份，不能用演示页证明真实账号生成链路。
- 当前原格式生成仍需 DOCX 原模板；PDF-only 输入的原位置重写/重建未实现，不能宣称所有输入格式都已交付。

### 服务器已完成的隔离准备

- 真实应用目录只读确认是 `/opt/zhitu-tracker`；现场健康检查 HTTP 200。没有修改正式环境变量、构建或账号。
- 官方 Debian 固定来源材料完整校验并上传 `/tmp/debian-trixie-slim-amd64.docker.tar`：29,796,352 字节，SHA256 `c63e038754d130aa5dd515070a9e2832aabf95373ac6ce83ce4104bf23f1baeb`。
- `docker load` 成功，镜像 ID `sha256:e426a54f50cc4cf82dd5cab8ba8426ed02c391840cb5a62dfd987542dbabea3b` 与校验配置一致；不含镜像标签覆盖。
- 转换代码和五份合成样例包 `/tmp/pdf-renderer-reviewed-20260908.tar` SHA256 `a31a4810d14034ffcaf5f71500132989e2a9bf3e7c42df0f6244ca3aef796398`，解到新的私有目录 `/opt/zhitu-pdf-qa-reviewed-20260908`；只链接现有 node_modules，不包含网站配置或用户资料。
- 服务器 `--check-fixtures` 通过：5 份、合计 26,271 字节、3 个实际运行模块预编译与安全档案检查成功。输出明确为 `realRenderPerformed:false`。
- 转换镜像构建已启动，限制 768MB 内存/无额外 swap/1 CPU，日志 `/tmp/zhitu-pdf-build-reviewed-20260908.log`；仅使用官方 Debian 源。基础镜像中安装约 192MB 的组件下载很慢，最后观察到第 23 个包（libreoffice-common），尚未得到最终转换镜像 ID。
- **尚未进行真实 DOCX→PDF 转换、中文/双栏视觉验收或正式部署。** 不因自动测试或预览器样例通过而放行。

### 继续时先做

1. 读取上述构建日志并检查 Docker 镜像，不重复启动同一构建。阿里云标签页最新终端为 `5f5y5kqb2h`；操作前核实连接。
2. 若构建成功，记录不可变镜像 ID，再在隔离目录运行 `node scripts/qa-resume-pdf-real.mjs --image=sha256:<实际ID> --docker=/usr/bin/docker --out=/tmp/zhitu-pdf-real-<新的目录>`；不得加载 `.env`。
3. 下载合成 PDF/检查报告，逐页转 PNG 并目视核验。通过后再做真实身份下网页生成→预览→双格式下载且额度不变的联调。
4. 用户已明确允许阿里云官方镜像站。仅构建时可选 `DEBIAN_MIRROR=aliyun-vpc`，使用阿里云文档规定的 VPC HTTP 地址；与基础镜像原 HTTP 传输方式相同，保持原 Debian Signed-By/keyring 与签名/哈希校验，不使用 `trusted=yes`、不关闭 TLS 验证、不改宿主机源。新包 SHA256 `4feddd35f7ec77bef49fb0035d0907cea4f98c454c879f26b634362b5880518a`，新日志 `/tmp/zhitu-pdf-build-aliyun-20260909.log`；先检查这个构建的最终结果。
5. 本地服务端口 3000 已确认 LISTENING、HTTP 200；但内嵌浏览器旧标签仍返回连接失败页。未绕过浏览器限制，不能宣称本地完整网页浏览器联调通过。独立端口的合成 PDF 预览器已实际显示成功。

## 2026-09-09 最新续验收：镜像构建成功，真实转换发现双栏提取误报（未发布）

本节替代上文“尚未实际转换/镜像仍在下载”的状态；不改变之前记录的历史事实。网页功能提交为 `7ebe6a1`，仍只在本地隔离分支，没有推送或部署。

- 在用户明确允许后，仅隔离镜像使用阿里云官方 VPC Debian 镜像站；APT 原有签名与哈希检查保留，宿主机源未修改。
- 构建成功：`zhitu-resume-pdf:reviewed-20260909`，不可变 ID `sha256:4cb43e89cc7c8d2eff9ec3f3fedf8ea5a13109cefd0cd0373a0b07ec720a41cc`。该镜像仍使用旧 `-layout` 提取，不能作为修复版发布。
- 真正运行了五份合成 DOCX 的受限容器转换，输出 `/tmp/zhitu-pdf-real-20260909-a`，每份约 1.2 秒。单栏 A4 与页眉页脚通过；两页样例正确返回 `PAGE_COUNT`；Letter 纸张正确返回 `PAGE_SIZE`；双栏表格返回 `TEXT_MISMATCH`，总结果不能标为通过。
- 五份样例均验证 `busyRejected`、`busyPrepareSkipped`、`preparedOnce`、`sourceUnchanged` 为真。没有调用 AI、网站配置或真实用户简历。
- 双栏实际输出是一页 A4（595.304 × 841.89 pt），PDF SHA256 `694ef6ea6848339ddcbc70f385601fa765faa47d494b96466b668faf2f146e26`。旧物理行提取在右栏句子的“个人”和“独立成果”之间插入了左栏 `Excel SQL Python`，触发严格原文连续性检查。尚未目视检查 PDF，不能据此断言排版已通过。
- 本地候选修复改用 Poppler 默认阅读顺序提取，去掉 `-layout`，不使用 `-raw` 兜底，不降低原文连续性和字符计数要求；4 项 Python 合约测试、72 项相关 TypeScript 测试及完整网站 75 文件/549 项回归通过，相关 lint 和 diff 检查通过。修复版尚未上传、重建或实际转换复验。
- 归档 `/tmp/zhitu-pdf-qa-results-20260909.tar` 只含上述合成样例和报告，SHA256 `2ad183fe6edf727b8ea6bb04e5adf27974e7d8bb335cde8b5f3e37483320f8c7`。阿里云文件下载跳转被内嵌浏览器 `ERR_BLOCKED_BY_CLIENT` 拦截，未绕过；已请用户下载此精确归档并提供本地文件。没有展开无关的服务器临时目录。
- 返回服务器工作台后出现“当前版本加载失败”，尝试官方“返回旧版”仍超时。不要重复执行未核实的终端粘贴命令；需要恢复后先读当前状态。

### 恢复后的验收顺序

1. 校验用户提供归档的 SHA256，安全展开到新的本地 QA 目录；用实际 PDF 对比默认阅读顺序提取与旧版提取。逐页渲染查看全部五份（含两页负例），不得将文本检查当视觉证明。
2. 上传本地修复的隔离转换代码、校验哈希，使用既有固定 Debian 基础镜像重建（可复用安装层缓存），记录新不可变镜像 ID。
3. 用新 ID 跑同一五份样例，输出新的 `/tmp/zhitu-pdf-real-20260909-b`；全部符合预期且页面目视通过后才继续。
4. 完成获授权的真实身份生成→预览→PDF/DOCX 下载联调与额度不变检查。仍然只支持 DOCX 原模板生成；PDF-only 重建尚未实现。
5. 上述条件通过后才推送网站发布、备份旧构建并部署；当前未启用正式 PDF 环境变量，未更换正式网页构建。

## 2026-09-09 02:40 最新状态：五份真实转换通过，视觉与网页联调仍待完成

用户重新连接 `5f5y5kqb2h` 后，继续在 `/opt/zhitu-pdf-qa-reviewed-20260908` 验收，没有修改正式网站配置、构建、账号或宿主机软件源。本节为最新状态。

### 实际发现与修复

- 默认 BuildKit 将裸 `sha256:<本地镜像ID>` 当远程镜像名解析，访问 Docker Hub 超时；随后使用 `DOCKER_BUILDKIT=0 docker build --pull=false`，继续从已校验的本地基础镜像构建。维持 768MB 内存/无额外 swap、CPU period/quota 100000/100000；不更改全局 Docker 设置。
- 默认阅读顺序候选镜像 `sha256:f204136408954ae1ca4019c0d515d768b61fd65b9eb752437a51d7239f0cfa84` 的实际输出目录 `...20260909-b` 仍有双栏 `TEXT_MISMATCH`。右栏“完成可复查的”和“字段说明”之间被插入左栏工具信息，不能发布这一候选版。
- 只读检查合成 PDF 的 `-bbox-layout` 坐标后，改用固定的栏目空白递归分组，不读取预期 DOCX 来选择顺序。所有文字必须保留，原有逐 run 与字符计数门槛不变；有歧义时仍可能拒绝，不能据此宣称全 ATS 或视觉通过。
- 首次坐标镜像 `sha256:14820c38d22be0ed2e7b69e1e36d2581413281b922052fee84258acce9a7a894` 因 Debian `python3-minimal` 缺少 `xml` 模块，`...20260909-c` 五份均正确以 `RENDER_FAILED` 失败，未向用户返回假文件。
- 在隔离镜像中安装完整 `python3` 标准库，并新增构建时 `import math, xml.etree.ElementTree` 检查。APT 仍使用用户批准的阿里云官方镜像站，Debian 签名与哈希验证不变。

### 最终服务器验收证据

- 最终镜像：`zhitu-resume-pdf:spatial-stdlib-20260909`，不可变 ID **`sha256:3a84c5d3db4e7980807aaeb643c5cf9dd33aa9083ba163dd4345656f1ea1c2ed`**。
- 构建日志：`/tmp/zhitu-pdf-build-spatial-stdlib-20260909.log`。
- 实际执行：`node scripts/qa-resume-pdf-real.mjs --image=sha256:3a84c5d3db4e7980807aaeb643c5cf9dd33aa9083ba163dd4345656f1ea1c2ed --docker=/usr/bin/docker --out=/tmp/zhitu-pdf-real-20260909-d`。
- `report.json` 返回 `passed:true`，完成于 `2026-09-08T18:39:26.364Z`。五份均保持原始 DOCX 不变，独占锁拒绝并发、跳过并发预处理、只准备一次的检查为真。

| 样例 | 实际结果 | PDF SHA256 |
| --- | --- | --- |
| 单栏 A4 | 1 页，595.304 × 841.89 pt，文字一致 | `0c1f2e5f9e959c2afc0380c84b5dbb07cac6e04f233a59a845d2ff9d7734863d` |
| 双栏表格 A4 | 1 页，595.304 × 841.89 pt，文字一致 | `bc3a0df653b16d4659d9a16a83b789c4664a8aed6191a37d35e8031d9164d2cf` |
| 页眉页脚 A4 | 1 页，595.304 × 841.89 pt，文字一致 | `6b5fb984d6465d0f69236215468f88cc0606f1ea924bcaf058ed8175f648ac43` |
| 两页溢出 | 正确拒绝 `PAGE_COUNT` | `d8dc16f1073f737c56c6970489a3964dd79a3446aa5c71f2c33a7f02f50233eb` |
| Letter | 正确拒绝 `PAGE_SIZE` | `3705e1bb53d69e9ae7ab37eba64f2371b9a3fce945de4dcde9a969fae63ad731` |

- 最新归档：`/opt/zhitu-pdf-qa-reviewed-20260908/zhitu-pdf-qa-final-20260909.tar`，SHA256 **`fce7c91fe8936b974d58791ed1dd41a40b2c3436daccd15869001f821366ca60`**。仅上述合成样例、报告与 QA 编译代码，无环境文件、密钥或用户资料。不要再让用户下载旧版失败包作最终验收。
- 本地最新完整回归：75 文件、549 项通过；10 项 Python 提取器测试在本机和服务器均通过；相关 lint、diff 检查通过。
- 最后从服务器读取正式首页 `HTTP 200`。没有发布网页、启用 PDF 配置、推送远端或重启正式应用。

### 剩余发布门槛（不得省略）

1. 已于下节完成用户下载归档的哈希核验与全部页面基础视觉检查；保留样例留白和验收覆盖范围限制。
2. 获授权的真实身份下完成生成→预览→PDF/DOCX 下载及额度不变联调；演示页、单独 PDF 画布测试不能替代。
3. 通过后正常推送分支/合并与备份部署，并配置上述实际镜像 ID。仍不支持 PDF-only 输入的原版式重建，不作全格式交付承诺。

## 2026-09-09 下载归档已收取：六页基础视觉检查完成，尚未发布

- 用户提供 `C:/Users/k'k/Downloads/zhitu-pdf-qa-final-20260909.tar`，本地 SHA256 与服务器记录 `fce7c91fe8936b974d58791ed1dd41a40b2c3436daccd15869001f821366ca60` 完全一致。
- 先检查归档条目为平面普通文件，无绝对路径、目录穿越或符号链接，再展开至隔离工作树 `tmp/pdfs/server-final-20260909`。没有执行包内的 QA 代码。
- 五份 PDF 的本地哈希均与上表一致，`pdfinfo` 页数和尺寸也与报告一致。全部以 110 DPI 渲染为 PNG，逐页查看，共六页。
- 单栏中文与英文显示正常，未见缺字方框、文字重叠或裁切；双栏表格中的长句在右栏内换行，没有跨栏覆盖；页眉页脚完整可见。两页负例保留第二页，Letter 负例仍为对应尺寸，不以截断或缩放伪装通过。
- 正常样例下半页有明显留白。本轮证明这些合成文件的基础显示及转换保护符合预期，不证明任意真实简历兼容，也不证明视觉饱满或全 ATS 验收通过；不得增造内容填满页面。
- 本地运行时没有 `pdffonts.exe`，未完成独立字体嵌入检查；不将肉眼中文字形正常表述为字体嵌入验证。
- 隔离工作树仅发现 `.env.example`，没有 `.env.local`。本机 3000 端口在监听，但不能把演示环境视为已登录测试用户的完整网站验收。
- 下一步仍须在获授权的测试身份和隔离环境中完成生成、预览、PDF/DOCX 下载及导出额度不变检查。本轮没有创建账号、修改数据库、读取密钥、推送或部署正式网站。

## 2026-09-09 真实网站身份与 DOCX 下载验收（PDF 全链路仍未完成）

- 用户明确允许独立测试环境临时账号、虚构简历测试与清理。复用固定测试库 `buczemvevbqnigfrvrlh` 的专用本机配置；没有读取正式 `.env.local`，测试子进程清空邮件、AI 及其他继承凭证。
- 在 `tmp/website-auth-20260909` 建立网站源码副本，仅使用 `http://127.0.0.1:3037`。保留其他端口的已有服务；临时测试脚本和报告位于隔离工作树的忽略目录 `tmp/`。
- 实际 HTTP 流程两轮通过：创建两个非管理员账号、网站密码登录、网站上传合成 DOCX、私有文件入库及真实解析、确认建议生成版本、DOCX 下载、原文件逐字节不变、跨账号 404、匿名 401。
- 分析结果为显式注入的 `synthetic_qa` 固定样例，不调用 AI；本轮验证“已有分析和确认建议之后”的生成链路，不能声称 DeepSeek 全链路测试通过。
- 下载 DOCX 的 `word/document.xml` 包含确认的新句、不再包含原句；原始文件下载字节与上传一致。下载响应为私有 no-store。生成、下载及 PDF 不可用失败前后，测试用户额度配置和 `ai_usage_tasks` 内容完全一致。
- 浏览器实际登录并打开简历中心；历史生成版本、确认建议与下载入口加载成功；点击“下载 DOCX”收到浏览器下载事件。点击“预览 PDF”在 Windows 测试实例返回真实 503，并显示转换服务尚未就绪、DOCX 已保留的失败提示，没有返回假 PDF。
- 从无历史版本开始的浏览器点击生成尚未通过：期间自动清理计时到期，测试用户已撤销并删除，后续生成请求为 401。此前真实 HTTP 生成已返回 200，但不能替代这一浏览器步骤。
- 第一轮运行 ID `c200e689-4133-4b83-a84e-102ce5cb41aa`；第二轮 `3c30c790-07f5-4827-9f75-5656b55d8808`。两轮共四个临时用户均完成全局会话撤销、私有文件清理和账号删除；没有操作其他测试账号或正式用户。
- 用户重连后，内嵌终端 `8pqgg77e89` 显示已连接，并只读确认 Node 22.23.2、约 1161MB 可用内存、29GB 磁盘余量及此前验收通过的不可变 PDF 镜像仍存在。仅新建空隔离目录 `/opt/zhitu-website-auth-qa-20260909`；尚未启动远程测试网站。
- `git archive` 打包 HEAD 中 `src public package.json pnpm-lock.yaml next.config.ts tsconfig.json next-env.d.ts`，不含环境文件、密钥、用户资料或未提交插件代码。包 `tmp/website-pdf-auth-20260909.tar` 大小 1515520 字节，SHA256 `bacf786bd73acd7b97c8c445de801d9019b8020d3cbd92d3589ccaed9e6e2271`。
- 上传该源码包到用户阿里云隔离目录时被安全审批明确拦截，要求此具体包与目的地的授权。未绕过、未改用其他传输通道。远程 PDF API 全流程、网页 PDF 预览成功与正式部署仍未完成。

## 2026-09-09 源码包获明确授权后上传，隔离依赖修正中

- 用户本轮明确允许上述源码包与阿里云隔离目录。通过 Workbench 文件选择上传成功，终端 SHA256 与本地 `bacf786bd73acd7b97c8c445de801d9019b8020d3cbd92d3589ccaed9e6e2271` 一致；只解包到 `/opt/zhitu-website-auth-qa-20260909`。
- 未读取正式环境文件。初次临时网站只使用测试项目公开连接配置，绑定 `127.0.0.1:3057`、内存上限 700M、CPU 75%、最长运行 3600 秒。检查发现正式目录中的 `pdfjs-dist` 为 5.4.296，与此源码要求的 6.2.108 不一致，已停止临时网站并移除测试目录中的依赖符号链接；正式依赖没有修改。
- 改为测试目录独立安装。服务器没有直接可执行的 pnpm，改用现有 Corepack 和项目固定的 pnpm 11.19.0。第一次锁定安装因源码归档漏带 `pnpm-workspace.yaml` 中 overrides 而明确失败，没有使用 `--no-frozen-lockfile` 绕过。
- 已补传原工作树的 `pnpm-workspace.yaml`（128 字节，本地 SHA256 `58b08923aba18ebe30c99081f956bfda22e6e43307d79ae6d5a12d4985c43fbc`）。完整发布包也必须包含此配置。重新安装的临时单元为 `zhitu-website-auth-deps-c-20260909`，内存 600M、CPU 75%、运行上限 1200 秒；须继续核对退出状态和依赖版本。
- 本地再次运行真实身份 HTTP 验收，运行 ID `9e21b267-793a-444b-b588-205ffe6fa852`：7 项通过，仍明确标记 PDF 未连接 Linux 转换器而未验证。登录页有内容、无错误日志；没有将浏览器登录尝试计作生成成功。
- 两个临时用户 `df9fddc0-7f8e-4704-b48c-88fa1778cb17` 和 `91a86001-0f0a-4690-9c3f-c000ea330b3b` 已清理；测试库 SQL 复核用户、会话、简历和私有文件残留均为 0。本轮本地 3037 服务已精确停止，其他本地网站不动。
- 远程操作持续有 20–50 秒延迟及超时。命令超时后先读取终端确认是否仅填入未执行，不盲目重复运行。当前没有推送 GitHub、部署正式网站或启用正式 PDF。
- 补传配置的服务器 SHA256 已核对一致。普通并发安装被终止后，使用 `NODE_OPTIONS=--max-old-space-size=320`、`--network-concurrency=1 --child-concurrency=1`，仍保留 600M 限额及 frozen lockfile，安装最终 `Result=success, ExecMainStatus=0`；实际 Next 16.3.0、pdfjs-dist 6.2.108。
- 完整本地归档 `tmp/website-pdf-auth-complete-20260909.tar` 已重建，额外包含 `pnpm-workspace.yaml`，SHA256 `c9e3b78cf40867ee7db3bd7e353614c7e48296db8ccc36e1142a3cc46d34172f`。服务器采用原包加单独配置补传，未再次上传完整新包。
- `zhitu-website-auth-qa-b-20260909` 在编译 `/` 时触发该测试单元自身的 cgroup OOM，上限 700M；内核记录 `CONSTRAINT_MEMCG`，没有提高限额。随后正式首页仍 HTTP 200。已切换到同样 700M 上限、320M Node 堆的接口专用临时实例 `zhitu-website-auth-api-20260909`，只准备 API 验收，不将之称为网页预览成功。
- 接口实例真实密码登录两次 HTTP 200、确认建议生成 HTTP 200；PDF 请求阶段再次发生该单元内的 cgroup OOM，返回到验收客户端的是 `fetch failed`。未获得成功 PDF 响应，不将之前的独立转换器通过结果代替此项。
- 本轮第二组临时测试运行 ID `18052ff3-abb4-4882-ac7a-d666e577369f`，测试用户 `57038ee7-67c9-4013-a9c0-28f97561e1b8`、`b552a8cf-388d-4f4c-828b-6ed3ebde094c` 已完成全局会话撤销和清理。生成与失败 PDF 前后额度任务及配置未变化。SQL 复核用户、生成版本和私有文件剩余均为 0。
- 不提高服务器测试资源上限，改用本机预构建。临时构建进程只注入测试库公开 URL、publishable key 和测试 APP_URL，不提供 service-role、AI 或邮件密钥。构建与 TypeScript 检查通过，36/36 页面完成；构建产物是测试环境版本，不可直接发布正式站。
- 预构建包 `tmp/website-pdf-prebuilt-test-20260909.tar` 为 8795648 字节，包含 `.next`、public、package.json 和 next.config.ts，排除 `.next/cache`、`.next/dev` 及 trace；不含环境文件或用户资料。计划只上传到既有 `/opt/zhitu-website-auth-qa-20260909`，在新子目录运行 Next start，以避开开发编译内存压力。
- 此预构建包上传被安全审批明确拒绝，理由为具体新包尚未获明确外传授权；已停止上传，没有改用命令编码或其他通道绕过。原源码包和配置已上传，预构建包尚未上传。等待用户对该具体包和隔离目的地确认。

## 2026-09-09 预构建包获准上传：服务器认证 PDF 接口通过，尚未发布

- 用户明确批准上传后，通过 Workbench 将 `website-pdf-prebuilt-test-20260909.tar` 上传至既有隔离目录。服务器 SHA256 `b0bffdaba474379f7c37eea3ad55d85f82f773a41157026efde1093015d9129d` 与本地一致；只展开到新建 `prebuilt` 子目录，链接父目录独立测试依赖，不使用或修改正式依赖。
- `zhitu-website-prebuilt-qa-20260909` 采用 Next start，绑定 `127.0.0.1:3057`，700M 内存、75% CPU、3600 秒运行上限，使用测试库公开配置及已验收的固定 PDF 镜像，无 service-role、AI 或邮件密钥。首页 HTTP 200，运行时内存读取为 135036928 字节；不再进行开发态编译。
- 本地预构建测试站最初受沙箱网络限制，登录返回 503；运行 `9f6a4fc2-f3fe-4c60-8b8d-7edc94086538` 的唯一临时用户 `aec16461-9804-46f8-babe-6bbfafab6132` 已撤销及删除。恢复已授权的独立测试库连接后重新验收，不将环境失败改成产品成功。
- 最新完整本地测试运行 `6fc9f882-0a56-4f8f-98f2-d17deb893000`：真实网站密码登录、合成 DOCX 上传和解析、基于固定合成分析的确认建议生成、DOCX 内容替换、原文件字节不变、匿名和跨用户下载拒绝均通过。没有调用 DeepSeek，不代表 AI 分析全链路验收。
- **服务器首次真实认证 PDF 导出通过**：两名非管理员真实密码登录 HTTP 200，确认建议生成 HTTP 200，PDF 实际返回 77549 字节，`application/pdf`、一页 A4、文字核验和私有 no-store 响应头均符合预期，文件以 `%PDF-` 开头。DOCX HTTP 200、另一账号 PDF 被拒绝、匿名 PDF 401。
- 服务端实际文件 `/opt/zhitu-website-auth-qa-20260909/authenticated-resume.pdf`，SHA256 **`d3b0c6ed8e414778c1453fe870c3ac1aaded15bb77d9fe41e2da68b4c1fa3ebe`**；报告为同目录 `authenticated-pdf-report.json`，`passed:true`。版本 ID `ed040b6f-9793-42e5-9ffc-80e3d29d8b93`。这里只包含合成内容，未上传用户真实简历；本轮未下载或目视检查此新 PDF，不将以前样例视觉检查替代本文件检查。
- 为验证首次生成，精确核对归属后清理本轮的两个一次性生成版本 `e30bcba9-07a1-4db5-bf8a-904ecf3b5963`、`ed040b6f-9793-42e5-9ffc-80e3d29d8b93`，保留合成源文件和固定分析。浏览器重新加载后确认 DRAFT、已接受 0/1、生成按钮禁用；点击接受建议后变成 1/1，再点击生成，页面变为 READY 并出现新版本 `9c36e1dd-59f7-4bcf-b03b-0bff0184c8ed`。点击下载 DOCX 得到实际浏览器 download 事件。
- 本地网页生成后的自动 PDF 请求如预期返回 Windows 转换器不可用，DOCX 和确认内容保留；不能据此声称浏览器 PDF 成功。网页控制多次出现 10–45 秒延迟和超时，用后续 AX 状态确认实际点击结果，没有盲目重复生成。
- 全部本地及服务器生成/导出、网页操作结束后，测试账号额度配置和 `ai_usage_tasks` 与开始时完全一致。两个临时用户 `45a34bb2-38aa-4259-af90-27a38fe04343`、`fb5daf31-af26-4aef-a473-cf8301c65406` 已全局撤销会话并清理。独立 SQL 复核包含失败轮用户在内，用户、会话、生成版本、私有文件残留均为 0。
- 已停止本轮本地 3037 进程和远程隔离服务；远程停止后 `ActiveState=failed, Result=exit-code`，日志确认在手动 stop 的 `16:00:05` 退出码为 143，属于关闭时终止状态，3057 已无监听；所读服务日志没有此前的编译内存溢出。正式首页再次 HTTP 200；本轮没有推送 GitHub、重启正式应用、修改正式配置或启用正式 PDF。

### 仍需完成

1. 浏览器直接访问服务器隔离测试站，完成实际 PDF 预览和下载成功测试；当前服务器站点仅监听服务器自身 loopback，本机浏览器不能直接连接。不能用本地失败分支或服务器独立 API 测试代替浏览器全链路；不得未经授权开放公网测试端口或新增远程访问凭证。
2. 下载上述认证生成 PDF 并做目视排版复核。
3. 全部验收后才能准备正式环境构建、备份与部署；当前测试预构建包内嵌独立测试库公开配置，绝不能直接用于正式网站。

### 临时 SSH 转发获授权后的连接检查

- 用户明确允许准备仅本机可访问的临时 SSH 转发，不开放公网端口、不修改正式站，若需认证由用户完成。
- 本机已安装 Windows OpenSSH；经获准的只读文件名检查，用户 `.ssh` 目录仅有 `known_hosts`，没有 `config` 或私钥文件，`SSH_AUTH_SOCK` 未设置。未读取任何私钥，也未新增密钥、授权项、服务器账号或防火墙规则。
- 浏览器库存连续两次返回 `nodeRepl.fetch request failed`，无法获得任何浏览器/终端当前状态。这是控制连接失败的证据，不是服务器已离线的证据；不要求用户反复登录来掩盖该区别。
- 当前尚未建立转发、重新启动测试服务或创建新测试账号。继续需要确认服务器公网地址、SSH 用户及用户本人完成 SSH 登录；保留主机身份核验，不关闭 StrictHostKeyChecking。

### 2026-09-09 18:07 更新：SSH 转发、真实 PDF 下载及目视检查通过

- 用户授权直接完成现有 SSH 密码认证；新连接认证成功，严格主机身份核验保持开启，仅绑定本机 `127.0.0.1:3057`。隔离网站首页实际 HTTP 200，没有开放公网测试端口或修改正式应用。
- 修正忽略目录中的连接辅助脚本：已运行且配置匹配的 QA 服务直接复用，不再以 exit 23 拒绝；只在本连接启动服务时负责结束清理。交互准备提示改为不回显按键，不在准备提示收集密码。密码没有写入脚本或本验收文档。
- 本轮运行 `55c5ca90-600c-4206-b754-9ce303cc4687` 的七项检查全部通过：两名非管理员真实网站登录、合成 DOCX 上传解析、确认建议生成、DOCX 仅替换确认文字且源文件不变、跨账号和匿名下载拒绝、实际服务器 PDF 下载、生成及成功导出前后额度不变。分析仍是固定合成样例，没有调用 AI。
- PDF 已真实下载到本地，77549 字节，SHA256 `4b0fe30de66659c0cfb628c33c838ead33a61b5a623ada5d56bd0d37ad33e845`。报告 `tmp/website-auth-tunnel-report.json`，文件 `tmp/pdfs/authenticated-tunnel-55c5ca90-600c-4206-b754-9ce303cc4687.pdf`。
- 本地 Poppler 独立核验一页 A4（595.304 × 841.89 pt），无 PDF JavaScript。渲染整页并目视检查：中英文可读、确认替换句出现、未见裁切重叠或缺字方框。合成内容较少导致下方留白，不代表任意真实简历或 ATS 验收已完成。
- 两个临时测试用户及其文件已清理，并撤销会话；不删除或修改正式用户。
- 浏览器控制库存再次返回 `nodeRepl.fetch request failed`，无法连接任何浏览器。因此尚未完成服务器版网页 PDF 预览/下载点击验证；这是浏览器控制通道阻塞，不是 SSH 或服务器 PDF 接口失败。停止重复要求用户登录服务器。本轮没有推送、正式部署或开启正式 PDF。

### 2026-09-09 18:52 测试页连接恢复

- 重启应用后 Windows Computer Use 能列出 Chrome 窗口，但该窗口当时显示非测试内容；读取被安全检查拒绝，未继续读取或绕过。已请用户在 Chrome 前台打开测试页。
- 用户反馈测试地址打不开。本机 3057 仍监听，绕过代理的 HTTP 请求也收到连接重置；服务器只读状态确认服务已在 18:34:15 被停止、退出 143，并非本轮读取证明的应用崩溃。
- 日志启动时间为 18:04:15，停止恰好在其 30 分钟后，与旧 SSH 辅助连接的清理计时一致：新的连接复用了旧服务，但旧连接仍可能到时停止它。辅助脚本存在跨连接生命周期冲突，不能将“端口仍监听”等同于服务可用。
- 已关闭本轮失效转发，重新通过配置核验的辅助连接启动隔离测试服务；18:52 新连接认证成功，本机直接 HTTP 200。仅恢复原有隔离测试服务，未改正式网站。本轮未新建测试账号，网页 PDF 按钮验收仍待测试页打开。

### 2026-09-09 用户手动网页 PDF 验收通过

- 因 Windows Computer Use 无法可靠确定浏览器 URL 而主动停止，用户同意改为本人操作、助手检查文件与后台结果；没有绕过浏览器安全检查。
- 运行 `8e1373a3-3f77-42fb-a053-1ff2e5f58e47` 在独立测试库创建两名临时非管理员，上传合成 DOCX 并用固定合成分析生成版本 `ed5d2af8-c3f2-4daf-8cee-b01ecd689fd0`。原文件不变、确认文字替换、DOCX/PDF 接口、跨用户及匿名访问拒绝均通过；没有调用 AI。
- 用户亲自登录测试网站，提供生成结果卡片及“下载一页 PDF”“预览 PDF”区域截图，并上传浏览器实际下载文件 `C:/Users/k'k/Downloads/示例公司_数据分析实习生_Synthetic-QA.pdf`。之后明确回答预览正文“可以”显示，并补充网页内实际 PDF 正文截图，确认预览不是仅展开空容器。
- 下载文件 SHA256 `9b4019280b0668a51b6e0f1d14d768a38c9767091659e2bf7feb43510330a907`，77549 字节，一页 A4，595.304 × 841.89 pt。独立文本提取包含已确认替换句、教育/实习/课程项目/工具与语言以及页末重复核验行。整页渲染复核中英文可读，未见裁切、重叠或缺字方框；合成简历的自然留白仍保留。用户原下载文件未被修改。
- 浏览器操作结束后的 `quota_unchanged_after_browser_actions` 通过；额度配置和额度任务记录与操作前完全一致。临时用户 `330f6819-9ace-4dc4-b9bb-905b0e25eb59`、`d881068e-a7d6-4222-be0b-e2a32846ce21` 的会话已全局撤销，账号和测试文件完成清理。
- **本合成 DOCX 场景的生成后网页预览及 PDF 实际下载验收完成**。网页点击由用户完成，助手核验截图、下载文件及后台结果，不表述为自动浏览器验收。仍不代表任意真实简历、PDF 原文件重建、全 ATS 或真实 AI 分析链路已验收。
- 下一阶段为正式环境构建、备份与部署；当前测试预构建包内嵌独立测试库配置，仍严禁用于正式发布。本轮没有推送、部署或开启正式 PDF。

## 2026-09-09 正式发布准备：额度即时刷新与后台精简

- 管理员额度 PATCH 成功后立即触发当前登录用户的 `/api/ai/quota` 无缓存读取，顶部显示服务器返回的剩余/总额度，不把总额度当作剩余次数。管理员给自己修改也走同一条链路。
- 页面重新可见时同步额度，保留原有聚焦刷新及 60 秒轮询；不同设备并非 WebSocket 实时推送。并发读取只采用最新请求结果，避免迟到的旧响应覆盖新次数。未修改正式用户额度或数据库权限。
- 按用户要求移除采集来源健康度 UI、后台入口相关文字，以及 overview 接口中的来源表读取。保留职位采集任务和历史数据，保留用户额度管理及反馈。
- 首次未限定目录的测试误包含临时源码副本和独立插件，出现缺少插件生成配置及重复 PDF 锁竞争；排除 `tmp` 与 `browser-extension` 后网站 76 文件、557 项通过。新增整页额度刷新测试随后通过，自改 20→50 时已用 3 次仍显示 47/50，切回页面同步 97/100。
- TypeScript、修改文件 ESLint 及 diff 检查通过。生产仍为 `b931c25`；正式 PM2 应用为 `zhitu-tracker`，目录 `/opt/zhitu-tracker`；维护 timer active；正式首页 HTTP 200。已核对固定 PDF 镜像仍存在。正式构建与部署结果在后续记录，不将准备检查视为已上线。
