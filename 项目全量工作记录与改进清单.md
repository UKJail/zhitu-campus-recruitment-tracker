# 职途 Tracker：项目全量工作记录与改进清单

> 更新日期：2026-08-14  
> 项目目录：`D:\求职追踪网页`  
> 生产站点：<https://zhitu-tracker.vercel.app>  
> 文档用途：记录截至目前已经完成的工作、当前真实能力、验证证据、已知不足和后续改进顺序。本文不包含任何 API Key、数据库密码、Webhook Secret、Service Role Key、简历正文或邮件正文。

## 1. 项目目标与产品边界

职途 Tracker 是面向中国大陆求职者的邀请制求职追踪 MVP。它试图把以下流程放在同一个产品内：

1. 邮箱验证码登录。
2. 上传和解析简历。
3. 浏览、筛选、收藏和比较职位。
4. 根据目标岗位 JD 分析简历匹配度。
5. 逐条接受或拒绝优化建议。
6. 在原始 DOCX 上生成可投递的岗位定制版本。
7. 跳转招聘官网投递，并由用户确认是否成功。
8. 接收或转发招聘邮件，识别测评、面试、Offer 和拒绝等进展。
9. 维护不可覆盖的申请事件时间线。
10. 完成面试记录与复盘。

当前明确不做：

- 自动代投。
- 托管猎聘、智联、前程无忧等招聘平台账号。
- 绕过登录、验证码、访问控制或反爬机制。
- 自动编造经历、技能、数字、学历或项目成果。
- 付费订阅、社区、浏览器插件和原生移动端。

## 2. 当前整体状态

| 模块 | 当前状态 | 说明 |
|---|---|---|
| Next.js Web 应用 | 已上线 | Vercel 生产站点可访问，真实账号可登录 |
| 邮箱验证码登录 | 已接通 | 使用 Supabase Auth 与 SSR 会话 |
| 邀请制与管理员 | 已接通 | 管理员可发邀请、查看来源健康度、调整 AI 配额 |
| 数据库与 RLS | 已接通 | 15 张业务表启用 RLS，普通用户按 `user_id` 隔离 |
| 简历上传与解析 | 已接通 | PDF/DOCX，私有存储，单文件不超过 10MB |
| DeepSeek 简历解析 | 已接通 | 有配额、运行记录、空结果重试与错误提示 |
| JD 匹配与建议 | 已接通 | 匹配分、缺失关键词、风险项、原文/建议/理由/影响 |
| 原格式定制简历 | 部分完成 | 仅 DOCX，可在原文件中替换已接受文字；PDF 不可原格式编辑 |
| 职位库 | 已接通但来源有限 | 当前主要来自 7 个 Greenhouse 企业公开职位源 |
| 投递确认与时间线 | 已接通 | 打开链接不计数，用户确认后才计入已投递 |
| Resend 招聘邮件识别 | 已接通 | 真实 Gmail 转发端到端测试通过 |
| 邮件提醒 | 部分完成 | 站内通知与定时字段已创建；真正的定时外发邮件尚未实现 |
| 面试复盘 | 已接通 | 已持久化到 Supabase，可新建与编辑 |
| 数据导出与注销 | 已接通 | 可导出 JSON；注销会删除文件并删除 Auth 用户 |
| 自动化测试 | 已通过 | 最新复核为 11 个测试文件、40 个测试通过 |
| 监控与灾备 | 未完成 | 仍需错误监控、告警和备份恢复演练 |

## 3. 技术架构

```text
浏览器
  └─ Next.js 16 / React / TypeScript（Vercel）
       ├─ Supabase Auth：邮箱验证码、会话
       ├─ Supabase Postgres：业务数据、RLS、状态函数
       ├─ Supabase Storage：私有简历原文件
       ├─ DeepSeek API：简历结构化与 JD 匹配建议
       ├─ Resend Receiving：招聘邮件入站
       └─ Railway Cron Worker
            └─ Greenhouse 公开 Job Board API → 职位标准化与去重 → Supabase
```

主要依赖：

- Next.js 16.3.0、React、TypeScript。
- `@supabase/ssr`、`@supabase/supabase-js`。
- `pdf-parse`、`mammoth`、`jszip`。
- `resend`、`zod`。
- Vitest、Testing Library、ESLint。

## 4. 已完成工作明细

### 4.1 登录页与认证

- 完成根目录独立 `index.html` 登录页参考实现：单文件、内联 CSS/JS/SVG/字体和视频海报，运行时只有游隼视频外部请求。
- 实现桌面、平板竖屏和手机三种响应式布局。
- 实现一次性入场动画、减少动态效果支持、焦点可见状态和手机滚动布局。
- 实际 Next.js 登录入口使用邮箱验证码，不使用密码表单。
- 首页日期改为访问者设备的真实本地日期；问候语按本地时间显示“早上好 / 中午好 / 晚上好”，且不再出现求职者姓名。
- 处理验证码位数与前端输入不匹配问题，最终已可成功进入账号。
- 使用 Supabase SSR 刷新会话，登录成功后进入 `/app`。
- 关闭产品层公开注册入口，采用管理员邀请制。

注意：独立 `index.html` 是设计参考页，并不是生产认证逻辑本身；生产入口由 `src/app/page.tsx` 提供。

### 4.2 Supabase 数据库、权限与存储

已创建 15 张核心业务表：

1. `profiles`
2. `invites`
3. `resumes`
4. `resume_versions`
5. `job_sources`
6. `jobs`
7. `saved_jobs`
8. `applications`
9. `application_events`
10. `inbound_emails`
11. `notifications`
12. `interviews`
13. `interview_reviews`
14. `ai_runs`
15. `source_runs`

已完成：

- 为全部业务表启用 RLS。
- 用户私有数据按 `auth.uid()` / `user_id` 隔离。
- `jobs`、`job_sources` 对登录用户只读。
- `invites`、`source_runs` 默认拒绝普通用户，供可信服务端使用。
- `application_events` 只追加，不允许更新或删除。
- 创建受控申请状态流转函数，状态变化自动写入事件。
- 创建用户注册后的 Profile 初始化函数。
- 默认每个用户最多保留 3 份活跃简历。
- 创建私有 `resumes` Storage bucket，限制 PDF/DOCX 与 10MB。
- Storage 路径按用户 ID 隔离，使用短时签名访问。
- 增加外键索引、AI 运行更新策略、手动外部申请和邮件确认函数。

已应用的迁移：

- `001_initial_schema.sql`
- `002_foreign_key_indexes.sql`
- `003_ai_run_updates.sql`
- `004_job_application_flow.sql`
- `005_inbound_email_actions.sql`
- `006_manual_external_applications.sql`
- `20260814071548_interview_review_persistence.sql`
- `20260814073831_application_status_transition.sql`

### 4.3 简历中心

已实现：

- 上传 PDF/DOCX。
- 文件类型、大小与数量校验。
- 原文件保存到私有 Supabase Storage。
- PDF/DOCX 文本提取。
- 简历列表、解析状态、结构化结果和版本历史。
- 删除单份简历：列表提供独立删除按钮，二次确认后同时删除私有存储原文件、解析记录和级联版本；接口再次校验当前用户所有权与存储路径。
- DeepSeek 结构化解析：基本信息、教育、经历、技能、项目和不确定项。
- DeepSeek 以“10 年资深 HR + 中国校招”为角色边界，覆盖学生身份、经历性质、个人贡献、技能熟练度、去 AI 味、面试可解释性、岗位匹配、缺失信息、风险和隐私规则。
- 强制保留原简历中符合 STAR 法则的真实情境、任务、行动、结果及其时间、范围、数量、百分比、金额、排名和成果数据；不得因精简、去 AI 味或匹配 JD 而删除或弱化。
- 明确要求 DeepSeek 不得编造事实；不确定内容标记为待确认或进入缺失信息/风险提示。
- 按最新产品决定移除了逐次勾选同意；页面保留静态说明，明确只发送解析文本、不发送原 PDF/DOCX，所有改写仍需用户逐条确认。
- 用户每日默认最多 20 次已完成 AI 操作，管理员可调整配额。
- `ai_runs` 记录类型、Provider、状态、输入指纹、输出或错误码。
- 针对 DeepSeek 返回空内容、截断 JSON 和资源不足情况增加校验、重试和可理解错误。

### 4.4 JD 匹配与简历优化

已实现：

- 输入目标公司、岗位名称和 JD。
- 选择当前简历后进行岗位匹配分析。
- 返回匹配分、缺失关键词、风险项和多条修改建议。
- 每条建议展示原文、建议文本、原因、影响等级及是否需真实性确认。
- 用户可以逐条接受或保留原文。
- 分析输入使用指纹绑定简历与 JD，避免把旧分析错误用于新 JD。
- 简历工作区接口会读取既有分析和版本，解决重新进入后完全丢失上下文的问题。
- 用户确认真实性后，才能基于已接受建议生成投递版本。

### 4.5 原格式岗位定制简历

用户提出“生成的简历必须保持原来的格式和字体”后，生成策略改为：

- 只接受原始 DOCX 作为可编辑模板。
- 不重新设计版式，不使用通用模板重排整份简历。
- 在 DOCX 的 `word/document.xml` 中定位建议对应的原文。
- 只替换用户已接受的文字，保留原文件字体、字号、段落、页边距和栏目结构。
- 保存为新的 `resume_versions`，保留目标公司、目标岗位、JD、分析运行 ID 和建议索引。
- 下载时重新从私有 Storage 读取原始 DOCX 并应用替换。
- 原文无法精确定位时中止生成并提示，而不是静默输出错误文件。
- PDF 明确拒绝“原格式编辑”，提示用户上传对应 DOCX。

该能力的真实边界：它能最大程度继承 DOCX 的原排版，但如果新文字明显更长，Word 仍可能自动换行或改变分页；目前还没有自动渲染 Word/PDF 进行逐页视觉回归。

### 4.6 职位库与采集 Worker

职位库已实现：

- 面向秋招应届生收敛为关键词搜索，以及公司、学历、经验、城市、招聘类型（应届生/实习）和收藏状态筛选；组合地点会拆分为独立城市选项。
- 匹配度、发布时间和公司名称排序。
- 每页 10 条的客户端分页。
- 收藏/取消收藏。
- 多职位并排比较。
- 打开原始投递链接。
- 受限来源说明，不对用户暗示可以绕过招聘平台限制。

Railway Worker 已实现：

- 独立 `JobSourceAdapter` / Greenhouse Adapter。
- 当前接入 IDEO、Adyen、AppLovin、Xendit、Eclipse Trading、AlphaGrep Securities 和 Rock Bund Capital。
- 通过公开 Greenhouse Job Board API 获取大中华区相关职位。
- 每两小时运行一次：`0 */2 * * *`。
- 按来源岗位 ID、规范化 URL、公司 + 职位 + 地点指纹去重。
- 保存运行状态、发现数量、新增数量和错误原因。
- 遇到 401、403、429 或结构异常时暂停对应来源，不影响其他来源。
- 猎聘、智联招聘和前程无忧保持受限/暂停，不绕过访问控制。
- 当前生产数据库曾验证包含 54 个职位；重复采集新增为 0。
- 修复 Worker 在数据库更新失败时没有正确暴露错误的问题。
- 增加陈旧 `running` 任务检查与修复脚本。

### 4.7 投递流程与求职进度

申请状态统一为：

- 已收藏 `saved`
- 准备投递 `preparing`
- 已投递 `applied`
- 测评 `assessment`
- 面试 `interview`
- Offer `offer`
- 已拒绝 `rejected`
- 已结束 `ended`

已实现：

- 点击“投递”先打开原招聘页面并创建“准备投递”。
- 用户返回后只有“返回”和“确认已投递”两个选项；返回只关闭弹窗并保留“准备投递”，只有确认成功才计入投递数量。
- 只有用户确认成功才计入投递数量。
- 从定制简历页面也可以手动记录外部岗位投递。
- 求职进度使用看板展示。
- 每次状态变化均追加事件，不覆盖历史。
- 受控状态机阻止不合理跳转。
- 用户可手动更新后续阶段。

真实腾讯校招岗位流程已经完成一次生产验证：岗位保存、定制简历下载、记录已投递和进度展示均可工作。

### 4.8 招聘邮件识别与通知

已完成 Resend Inbound 接入：

- 每个用户生成独立入站别名。
- Resend 托管收件域名已配置。
- Webhook 地址：`https://zhitu-tracker.vercel.app/api/webhooks/resend`。
- 验证 Svix 签名、时间戳和消息 ID。
- 通过 Resend Receiving API 拉取邮件正文。
- 按 Provider 邮件 ID 去重。
- 将邮件分类为投递确认、测评、面试、Offer、拒绝或其他。
- 提取公司、岗位、时间、会议链接和截止时间。
- 尝试匹配现有申请。
- 高置信度结果进入通知中心。
- 涉及状态变化时只创建待确认建议，不直接修改申请。
- 用户可以接受或忽略建议。
- 用户可以删除单封邮件敏感内容。

生产端到端验证已完成一次：

```text
Gmail 转发真实测试邮件
  → Resend 接收
  → Webhook 签名通过
  → 分类为面试
  → 匹配腾讯岗位
  → 生成待确认通知
  → 用户确认
  → 申请从已投递变为面试
  → 写入不可覆盖事件
```

测试结束后，在用户授权下清理测试数据。由于申请事件设计为不可变，原测试事件不能删除，因此追加了一条反向补偿事件，把申请恢复到“已投递”；测试通知已删除，测试邮件正文等敏感字段已软删除/脱敏，同时保留去重墓碑。

### 4.9 面试复盘

已实现并持久化：

- 公司、岗位、轮次和日期。
- 面试官。
- 问题记录。
- 回答摘要。
- 表现亮点。
- 待改进项。
- 后续任务。
- 自评分。
- 下轮准备。
- 新建、编辑和列表查询。
- 数据只对当前账号可见。

### 4.10 通知、设置、隐私和管理员后台

已实现：

- 全局通知中心。
- 邮件跟踪设置与专属地址复制。
- 入站邮件列表和单封敏感内容删除。
- 全部个人数据 JSON 导出。
- 输入“注销”后执行账号注销。
- 注销前删除用户简历文件，再删除 Supabase Auth 用户，由级联关系清理业务数据。
- 管理员邀请用户。
- 管理员调整用户每日 AI 配额。
- 管理员查看采集来源健康度和任务结果。
- 管理端不直接展示用户简历正文或邮件正文。

### 4.11 部署与外部服务

已完成：

- Web 应用部署到 Vercel。
- Vercel 生产域名正常指向主部署。
- Supabase 生产项目连接。
- Railway Worker 部署与两小时 Cron。
- Resend API、Inbound 域名和 Webhook 接入。
- DeepSeek API 接入。
- Vercel 生产环境变量配置。
- Railway 服务环境变量配置。
- Vercel Deployment Protection 调整，生产站可直接访问。
- 修复 Vercel 项目重新连接后的部署访问问题。
- 安装并登录 Vercel CLI、Railway CLI，完成相关部署操作。

所有 Secret 都应只保存在 Vercel、Railway、Supabase 或本地 `.env.local` 中，不得写进本文、客户端变量或代码仓库。

## 5. API 路由清单

### 账号与认证

- `GET /auth/callback`
- `DELETE /api/account`
- `GET /api/account/export`
- `GET /api/health`

### 简历与 AI

- `GET|POST /api/resumes`
- `DELETE /api/resumes/[id]`
- `GET /api/resumes/[id]/workspace`
- `GET /api/resumes/versions/[id]/download`
- `POST /api/ai/parse-resume`
- `POST /api/ai/analyze`
- `POST /api/ai/generate-resume`

### 职位与申请

- `GET /api/jobs`
- `POST /api/jobs/[id]/save`
- `POST /api/jobs/[id]/prepare`
- `POST /api/applications/[id]/result`
- `POST /api/applications/[id]/status`
- `POST /api/applications/manual`

### 邮件与通知

- `GET /api/inbound-address`
- `GET /api/inbound-emails`
- `DELETE /api/inbound-emails/[id]`
- `GET /api/notifications`
- `POST /api/notifications/[id]/action`
- `POST /api/webhooks/resend`

### 面试复盘

- `GET|POST /api/interview-reviews`
- `PATCH|DELETE /api/interview-reviews/[id]`

### 管理员

- `GET /api/admin/overview`
- `POST /api/admin/invites`
- `PATCH /api/admin/users/[id]/quota`

## 6. 测试与验证记录

### 6.1 本次文档整理时重新执行

2026-08-14 最新本地复核结果：

- `pnpm test`：11 个测试文件通过，40 个测试通过。
- `pnpm lint`：通过。
- `pnpm build`：Next.js 16.3.0 生产构建通过，21 个页面/路由完成生成或注册。
- `pnpm worker:build`：Worker TypeScript 构建通过。

### 6.2 已覆盖的单元测试主题

- 职位去重与 URL 规范化。
- 受限来源暂停。
- Worker 数据库更新失败处理。
- 申请状态流转与投递计数。
- AI Provider 正常、空内容、截断和资源不足返回。
- AI 建议原文保留和用户决定。
- 邮件分类映射。
- 面试复盘字段校验。
- DOCX 原文替换、空格保留和找不到原文时失败。
- 简历工作区恢复。

### 6.3 已完成的生产验证

- 生产站登录成功。
- 腾讯岗位可见并可记录已投递。
- DeepSeek 真实简历解析和 JD 匹配有成功输出记录。
- 定制 DOCX 可生成和下载。
- Railway 公开职位采集成功，重复采集新增为 0。
- 未登录访问管理接口、入站地址接口返回 401。
- 未签名访问 Resend Webhook 返回 401。
- 真实招聘邮件入站、分类、匹配、用户确认和事件更新闭环通过。

## 7. 处理过的问题与修复结果

| 问题 | 原因/表现 | 处理结果 |
|---|---|---|
| 验证码位数不一致 | 邮件收到 8 位验证码，前端按其他长度处理 | 前端已调整，可成功登录 |
| 简历一直显示生成中 | 异常没有及时回收前端状态或服务返回异常 | 增加错误处理，按钮状态可恢复 |
| DeepSeek 返回空内容 | 模型可能返回空字符串、截断 JSON或资源不足 | 加入输出校验、重试和诊断错误 |
| 生成简历破坏排版 | 通用模板重建无法保留原格式 | 改为在原 DOCX 内替换已接受文字 |
| 重新进入需重填 JD | 分析上下文只保存在当前组件状态 | 增加简历工作区读取与历史恢复 |
| 投递状态写入受权限限制 | RLS 与前端直接更新流程不完整 | 增加数据库受控流转函数和迁移 |
| Railway CLI 无法安装 | PowerShell 执行策略阻止 `npm.ps1` | 使用 `npm.cmd` 安装并验证版本 |
| Worker 残留 running | 任务更新错误未被正确检查 | 增加错误检查和陈旧任务修复脚本 |
| Resend 集成没有域名选项 | Resend 尚无自有已验证域名 | 使用 Resend 托管接收域名完成 MVP |
| 测试邮件会改变真实岗位状态 | 生产 E2E 使用了真实申请记录 | 用户授权后追加补偿事件恢复状态并清理敏感测试内容 |

## 8. 已知不足与风险

以下内容是下一阶段最重要的改进依据。

### 8.1 P0：上线安全与可靠性

1. **轮换曾暴露过的 DeepSeek Key。** 密钥曾在聊天中出现过，即使用户要求忽略，也应视为已经暴露。需要在 DeepSeek 控制台吊销旧 Key、创建新 Key，并同步更新 Vercel 后重新部署。
2. **完成 Supabase 备份恢复演练。** 当前有数据库，但没有形成“备份可恢复”的证据。
3. **接入错误监控与告警。** 当前主要依赖平台日志，缺少统一的前端异常、API 失败、Webhook 失败和 Worker 失败告警。
4. **做两用户数据隔离验收。** RLS 已配置，但仍需用两个真实测试账号逐项尝试读取对方简历、申请、邮件和复盘。
5. **建立 Secret 轮换制度。** Supabase Service Role、Resend Webhook Secret、Resend API Key 和 DeepSeek Key 应有定期轮换记录。
6. **增加速率限制和滥用防护。** 登录验证码、AI、邀请、Webhook 和下载接口需要更明确的速率限制策略。

### 8.2 P1：产品真实性与核心体验

1. **账号姓名仍有硬编码。** `tracker-app.tsx` 顶部账号区域显示固定姓名，应改为读取 `profiles.display_name` 或登录邮箱。
2. **“绑定邮箱”目前实际是专属转发地址。** 这不是 Gmail/Outlook OAuth 绑定；文案应改为“设置招聘邮件转发”，或后续真正实现 OAuth 邮箱连接。
3. **定时邮件提醒尚未真正发送。** 目前有 `scheduled_for` 与站内通知，但没有发现调用 `resend.emails.send` 的提醒调度器。
4. **职位提醒按钮是占位功能。** 当前点击只提示“将在邮件通知阶段接入”，尚无订阅规则和定时推送。
5. **职位匹配分并非所有真实职位都已逐一计算。** 真实职位列表中未分析岗位会显示待分析或默认值，需设计批量匹配与成本控制。
6. **职位采集覆盖不足。** 当前主要是 7 个 Greenhouse 来源，和“覆盖更多中国企业官网、公开聚合源”的产品目标仍有明显差距。
7. **真实职位字段质量不稳定。** Greenhouse 源通常缺少标准薪资、经验和学历，筛选价值会受影响。
8. **投递链接有效性需要持续检测。** 当前采集可更新职位，但还需处理过期、下架、重定向和失效链接。
9. **申请详情页不足。** 当前主要是看板卡片，缺少单个申请的完整详情、事件时间线、联系人、附件和下一步任务视图。
10. **面试复盘关联不足。** 数据模型支持关联方向，但 UI 还没有完整选择并展示岗位、申请和简历版本关联。

### 8.3 P1：简历与 AI

1. **PDF 无法原格式生成。** 当前必须上传 DOCX；需要明确引导用户保存 Word 原稿，或研究安全可控的 PDF 表单/文本层编辑。
2. **DOCX 只做文本替换。** 页眉、页脚、文本框、SmartArt、复杂表格、域代码和跨段落文字可能无法定位。
3. **替换后缺少视觉 QA。** 字体样式会继承，但更长的文字可能换行、溢出或改变分页；需要 DOCX → PDF 渲染和逐页截图检查。
4. **建议接受状态的持久化还不够清晰。** 需要明确每条建议的数据库实体、接受/拒绝时间、操作者和对应生成版本，而不只依赖分析输出与版本元数据。
5. **“生成投递简历”当前不再调用 DeepSeek 重写整份内容。** 它只把此前已接受的 DeepSeek 建议应用到原 DOCX；这符合保真要求，但需要在 UI 中解释，避免用户期待整份自动重写。
6. **DeepSeek 模型和结构化输出需持续回归。** 模型版本、最大输出、重试策略和成本尚未形成监控面板。
7. **敏感数据处理告知仍需制度化。** 当前按产品决定不再要求逐次勾选，但正式运营前仍需在隐私政策中明确 DeepSeek 处理范围、保存期限、法律依据、撤回和删除方式，并记录政策版本。

### 8.4 P1：邮件识别

1. **自动转发配置体验不完整。** Gmail 自动转发通常需要确认邮件；产品内尚没有专门展示确认链接/确认正文的流程。
2. **分类器样本有限。** 已通过一封真实测试邮件，不代表测评、Offer、拒绝、多个岗位同公司、转发嵌套等场景都准确。
3. **岗位匹配可能歧义。** 同一公司多个相似岗位时，自动匹配需要让用户选择目标申请。
4. **时区、中文日期和会议链接解析需扩充。** 例如“明天下午”“本周五前”“北京时间/香港时间”都需固定样本测试。
5. **撤销体验需要产品化。** 数据层可通过补偿事件恢复，但 UI 应提供明确“撤销这次更新”并解释时间线不会被删除。
6. **邮件原文保存策略需更细。** 应支持默认保存周期、自动清理、仅保留结构化结果等用户选项。

### 8.5 P2：界面、移动端与可访问性

1. 主应用组件 `tracker-app.tsx` 约 967 行，职责过多，后续修改容易互相影响，应拆分页面、状态 Hooks 和 API Client。
2. 需要对五个主模块做系统性手机真机验收，目前“能响应式显示”不等于“体验完整”。
3. 看板和职位表在窄屏上的信息密度、横向滚动和触控目标需要重新设计。
4. 通知弹层、比较弹层、复盘弹层需要完整的焦点锁定、Esc 关闭和焦点返回。
5. 登录页参考稿与生产登录页视觉不是完全同一个实现，需决定最终品牌方向并统一。
6. 字体自托管策略尚未对生产主应用形成清晰资产清单。
7. 空状态、加载骨架、离线提示和网络重试可进一步统一。
8. 缺少深色模式（如果产品决定需要）。

### 8.6 P2：工程与运维

1. 当前目录未发现 Git 元数据，无法通过 `git status` 和提交历史追踪改动；应确认正式 GitHub 仓库是否在其他目录，或为当前目录初始化并连接远端。
2. 依赖大量使用 `latest`，可重复构建风险较高；应固定 Next.js、React、Supabase、Zod 等版本并配置依赖升级流程。
3. 缺少正式的端到端自动化测试套件。当前真实流程主要靠人工生产验收。
4. 缺少预发布环境和隔离的测试数据库/Resend 测试配置。
5. 缺少 CI：应在提交时自动执行 lint、test、build 和 Worker build。
6. 缺少数据库迁移状态自动核对与回滚/前滚手册。
7. 缺少结构化日志、请求追踪 ID、错误聚合和关键指标看板。
8. 账号注销当前是同步操作，不是原方案中的异步删除任务；大文件或服务失败时需要可恢复的删除工作流。
9. 数据导出为 JSON，普通用户阅读和迁移不方便，可增加 ZIP、CSV 和文件打包。
10. Railway `WORKER_INTERVAL_MS` 仍保留，但生产实际由 Cron 触发，应统一配置语义，避免误解。

## 9. 推荐改进路线

### 第一阶段：先稳住真实使用（P0，1～3 天）

- [ ] 轮换 DeepSeek Key，并重新部署验证。
- [ ] 接入错误监控（建议 Sentry）和 Vercel/Railway 失败告警。
- [ ] 执行 Supabase 备份恢复演练并记录结果。
- [ ] 创建第二个测试用户，逐项验证 RLS 隔离。
- [ ] 把账号显示从硬编码改为真实 Profile。
- [ ] 把“绑定邮箱”文案改成真实能力描述。
- [ ] 为生产核心流程建立一份人工冒烟测试清单。

### 第二阶段：补齐核心闭环（P1，3～7 天）

- [ ] 实现真正的定时邮件提醒发送 Worker。
- [ ] 完成职位提醒订阅：关键词、城市、来源、频率和退订。
- [ ] 为单个申请增加详情与完整事件时间线。
- [ ] 增加邮件状态更新撤销按钮和歧义匹配选择。
- [ ] 将面试复盘真正关联申请、职位和简历版本。
- [ ] 为 DOCX 生成加入渲染预览和分页/溢出检查。
- [ ] 增加更多公开企业官网适配器，并建立字段质量评分。

### 第三阶段：小规模邀请测试（1～2 周）

- [ ] 邀请 3～5 位真实用户，而不是立即扩到 30 人。
- [ ] 记录上传解析成功率、AI 建议接受率、职位有效率、投递确认率和邮件识别准确率。
- [ ] 收集每位用户在五个主模块中的阻塞点。
- [ ] 修复 P0/P1 问题后再扩到 10～30 人。
- [ ] 明确隐私政策、数据保存期、删除机制和跨境数据告知。

### 第四阶段：公开运营准备

- [ ] 建立开发、预发布、生产三套环境。
- [ ] 完成隐私政策、用户协议和敏感数据授权记录。
- [ ] 评估中国大陆访问速度、境内部署、备案和数据跨境要求。
- [ ] 建立成本预算：Vercel、Supabase、Railway、Resend 和 DeepSeek。
- [ ] 建立故障响应、密钥轮换和数据恢复制度。

## 10. 建议的下一次验收清单

每次发布后至少验证：

- [ ] 新邀请用户可以收到验证码并登录。
- [ ] 未登录用户无法访问 `/app` 私有数据。
- [ ] 用户 A 无法读取用户 B 的简历、申请、邮件和复盘。
- [ ] PDF/DOCX 文件限制生效。
- [ ] DeepSeek 数据处理说明、配额、成功和异常提示都正确。
- [ ] 新 JD 不会误用旧分析。
- [ ] DOCX 定制版本只应用已接受建议。
- [ ] 下载后的字体、段落和分页经过人工视觉检查。
- [ ] 打开投递链接不会直接增加投递数。
- [ ] 确认成功后投递数只增加一次。
- [ ] 状态变化追加事件且不能被覆盖。
- [ ] 重复邮件不会生成重复事件。
- [ ] 邮件识别错误可以忽略、修正或撤销。
- [ ] 测评/面试提醒在正确时间发送。
- [ ] 面试复盘保存后刷新仍存在。
- [ ] 导出数据完整，注销账号后文件和数据不可再访问。
- [ ] 桌面、平板和手机均可完成五个核心流程。
- [ ] Vercel、Railway 和 Webhook 错误能触发告警。

## 11. 常用命令

```powershell
# 本地开发
pnpm dev

# Web 质量检查
pnpm test
pnpm lint
pnpm build

# Worker
pnpm worker:build
pnpm worker:probe
pnpm worker:verify
pnpm worker:verify-inbound
pnpm worker:repair-runs
```

本地开发地址：`http://localhost:3000`。生产地址：<https://zhitu-tracker.vercel.app>。

## 12. 文件导航

| 路径 | 作用 |
|---|---|
| `src/app/page.tsx` | 生产邮箱验证码登录页 |
| `src/app/app/page.tsx` | 登录后的应用入口 |
| `src/components/tracker-app.tsx` | 当前五个主模块及全局弹层 |
| `src/app/api/` | Next.js API 路由 |
| `src/lib/ai/provider.ts` | DeepSeek Provider、结构化输出和事实约束 |
| `src/lib/resumes/parse.ts` | PDF/DOCX 文本解析 |
| `src/lib/resumes/template-docx.ts` | 原 DOCX 格式保真文字替换 |
| `src/lib/mail/classifier.ts` | 招聘邮件分类和字段提取 |
| `src/lib/supabase/` | Supabase Client、Server、Admin 和类型 |
| `supabase/migrations/` | 数据库结构、RLS、函数和迁移 |
| `worker/` | Railway 职位采集 Worker |
| `railway.json` | Railway 构建、启动和 Cron 配置 |
| `index.html` | 独立像素锁定登录页参考稿 |
| `README.md` | 项目快速启动和部署摘要 |

## 13. 交接结论

当前项目已经不是纯演示稿：登录、数据库、私有简历、DeepSeek、岗位采集、投递状态、Resend 入站邮件、通知确认、面试复盘和生产部署都已有真实链路，其中核心投递与邮件更新流程完成过生产端到端验证。

但它仍然是 MVP，不应直接扩大公开使用。当前最需要做的不是继续堆页面，而是先完成密钥轮换、备份恢复、错误监控、双用户隔离测试，并修复硬编码账号、邮件提醒未真正发送、职位提醒占位、职位来源不足和 DOCX 缺少视觉 QA 等问题。完成这些后，再邀请 3～5 位真实用户进行一周的小规模测试，依据实际阻塞点决定下一轮功能优先级。

## 14. 2026-08-14：无域名邀请链接

- 管理员后台保留“发送邮件”，新增“生成邀请链接并复制”。
- 链接由 Supabase Auth 服务端生成，不依赖 Resend 发信域名；前端会尝试自动复制，失败时保留只读文本供手动复制。
- 新增 `/auth/confirm` 验证入口：仅接受 `invite` 类型的一次性令牌，验证成功后建立会话并进入 `/app`。
- 可用令牌不写入业务表，`invites.token_hash` 只保存再次哈希后的摘要；链接使用后更新 `used_at`。
- UI 明确提示邀请链接只发给对应邮箱，默认约 1 小时内使用。
- 本轮验证：`npm.cmd test`（41/41）、`npm.cmd run lint`、`npm.cmd run worker:build`、`npm.cmd run build` 均通过。

## 15. 2026-08-15：测试版邀请激活与密码登录

- 停用依赖 SMTP 的邀请邮件和邮箱验证码登录，测试阶段不再受 Resend 未验证域名限制。
- 管理员生成 256 位随机一次性激活链接，业务表只保存 SHA-256 摘要，链接 24 小时有效。
- 受邀者在 `/activate` 输入受邀邮箱并设置密码；服务端校验链接、邮箱、有效期和使用状态。
- 激活接口使用服务端管理员权限创建账号；对已被旧邀请流程创建的账号，可重新设置密码并完成修复。
- 令牌通过条件更新原子占用，账号创建失败时释放占用；成功后不可重复使用。
- 登录页切换为邮箱和密码登录，不再显示或发送六位验证码。

## 16. 2026-08-15：用户 ID 与账号管理

- 删除侧边栏中硬编码的管理员姓名，改为读取当前登录用户自己的 `profiles.display_name`。
- 未设置资料时显示“未设置用户 ID”，头像只使用当前用户 ID 的首字符。
- 激活账号时由受邀者自行设置 2—24 字符的用户 ID，后续可在账号面板修改。
- 左下角“账号与管理”面板支持修改用户 ID、验证当前密码后修改登录密码，以及退出登录。
- 管理员控制台入口仅向 `is_admin=true` 的当前用户显示，普通用户不会看到管理员入口或管理员资料。
